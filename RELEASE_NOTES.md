# Release Notes

## Recent Changes

- **Jira board adapter**: Added Jira integration with keychain token support for seamless project management
- **GitLab board adapter**: Implemented `GitLabBoardAdapter` with full `BoardAdapter` interface
- **Opencode adapter**: Added `OpencodeAdapter` with exit code tracking and tests
- **Per-step timeouts**: Added per-step timeout and hang detection to the workflow runner
- **Run reporting**: Added heartbeat, estimates, artifacts, and step events for better observability
- **Event-driven polling**: Replaced `setInterval` with event-driven resolution in `waitForCompletion`
- **Git setup in dev-workflow**: Moved git setup to Step 0 to ensure a clean branch before work begins
- **Agent prompt extraction**: Extracted inline agent prompts into standalone files for maintainability
- **Bug fixes**: Restored `oflow-ready` label on task failure; wired `writeSessionsJson` into daemon loop so `status` command works correctly
- **Refactors and cleanups**: Extracted duplicate utilities, removed unused config params, removed dead code

_Generated by oflow e2e verification._
