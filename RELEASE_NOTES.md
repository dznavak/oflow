# Release Notes

## Recent Changes

- **GitLab board adapter**: Added full `BoardAdapter` implementation for GitLab Issues, supporting label-based workflow management (`OFLOW_BOARD=gitlab`)
- **Opencode agent adapter**: Implemented `OpencodeAdapter` with accurate exit code tracking, unit tests, and README docs (`OFLOW_AGENT=opencode`)
- **Run reporting**: Daemon now shows `[active]`/`[done]` status with token counts; added `oflow report estimate` command, auto-emitted artifact events, and step-level reporting across skill files
- **Multi-agent code review skill**: Added parallel specialist agents (logical, smells, tests, assembly) extracted into standalone `.claude/agents/` files for modular orchestration
- **Dev-workflow improvement**: Git setup moved to Step 0 so branches are always created from a clean, up-to-date main
- **Dynamic version**: CLI version now read from `package.json` instead of being hardcoded, preventing drift
- **Schema consolidation**: Merged split schema files into a single `schemas.ts` for simpler imports
- **Bug fixes**: Fixed session loop on duplicate `oflow-in-progress` label removal, corrected `--fork-point` argument order in code-review diff, and resolved lint issues from unused imports

_Generated by oflow e2e verification._
