# Release Notes

## Recent Changes

- **Multi-agent code review**: Added a parallel code review skill that fans out to three specialist agents (logical correctness, code smells, test quality) and assembles a deduplicated final report
- **Agent prompt extraction**: Refactored inline agent prompts into standalone `.claude/agents/` files, making the multiagent-code-review skill a lean orchestrator
- **Code review improvements**: Updated the code-review step with merge-base diff, review principles, and fixed `--fork-point` argument ordering with fallback
- **Schema consolidation**: Merged duplicate schema files into a single `schemas.ts` for simpler maintenance
- **Daemon reliability**: Fixed `OpencodeAdapter` to hold the Node.js event loop open for child processes, preventing silent process loss; improved spawn logging
- **Label-based filtering**: Added label-based task filtering for `board list`, `board pick`, and `run` commands
- **Skills restructured**: Reorganised skills as folders with `SKILL.md` per agentskills.io format
- **Bug fixes**: Fixed Zod validation for `readTaskContext`, agent log reliability, skill path resolution, and session loop when `oflow-in-progress` label is already removed
- **README added**: New installation and usage documentation at the repo root
- **E2E verification**: Added e2e verification step to the dev-workflow skill

_Generated by oflow e2e verification._
