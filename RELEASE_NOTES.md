# Release Notes

## Recent Changes

- **fix:** Wired `writeSessionsJson` into the daemon loop so the `status` command correctly shows active sessions instead of "No active sessions found"
- **fix:** Restored `oflow-ready` label on task failure instead of incorrectly applying `oflow-done`
- **feat:** Moved git setup to Step 0 in `dev-workflow` skill to ensure a clean branch start before any exploration or code changes
- **feat:** Implemented `GitLabBoardAdapter` with full `BoardAdapter` interface, supporting label-based workflow management and GitLab Issues as a task board
- **feat:** Implemented `OpencodeAdapter` with accurate exit code tracking and unit tests, enabling `OFLOW_AGENT=opencode` support
- **feat:** Added run reporting — daemon shows `[active]`/`[done]` status with token counts, `oflow report estimate` persists complexity/timing, and artifact events are auto-emitted from `StateManager`
- **refactor:** Extracted inline agent prompts from `multiagent-code-review` skill into standalone `.claude/agents/` files for cleaner orchestration
- **feat:** Added `multiagent-code-review` skill dispatching parallel specialist agents (logical, smells, tests, assembly)
- **chore:** Replaced hardcoded version string in CLI with dynamic read from `package.json` to prevent version drift
- **docs:** Added README with installation and usage instructions

_Generated by oflow e2e verification._
