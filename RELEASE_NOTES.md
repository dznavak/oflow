# Release Notes

## Recent Changes

- **Multi-agent code review**: Added parallel specialist code review skill that fans out to three reviewer agents and assembles one final report
- **Agent prompt extraction**: Refactored inline agent prompts into standalone files for better maintainability
- **Opencode adapter fix**: Removed `child.unref()` from OpencodeAdapter to align behaviour with ClaudeCodeAdapter
- **Schema consolidation**: Merged duplicate schema files into a single `schemas.ts` module
- **E2e verification**: Added e2e verification step to dev-workflow for end-to-end confidence
- **Label-based filtering**: `board list`, `board pick`, and `run` now support filtering tasks by label
- **Daemon reliability**: Fixed session spawn logging, Claude stdin invocation, and log streaming
- **Bug fixes**: Resolved Zod validation for `readTaskContext`, fixed stuck-session loop on pre-existing labels, and corrected merge-base diff argument order in code-review step
- **Version sourcing**: CLI version is now read dynamically from `package.json` instead of being hardcoded
- **README**: Added installation and usage documentation

_Generated by oflow e2e verification._
