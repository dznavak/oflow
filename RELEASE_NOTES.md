# Release Notes

## Recent Changes

- **fix:** Wire `writeSessionsJson` into daemon loop so the `status` command correctly reflects active sessions
- **fix:** Restore `oflow-ready` label on task failure instead of incorrectly applying `oflow-done`
- **feat:** Move git setup to Step 0 in `dev-workflow` to ensure a clean branch before work begins
- **feat:** Implement `GitLabBoardAdapter` with full `BoardAdapter` interface for GitLab support
- **feat:** Implement `OpencodeAdapter` with exit code tracking and tests
- **feat:** Add run reporting with heartbeat, estimates, artifacts, and step events
- **feat:** Add multi-agent code review skill with parallel specialist agents (logical, smells, tests)
- **refactor:** Extract inline agent prompts into standalone agent files for better maintainability
- **chore:** Read version from `package.json` instead of hardcoding in CLI
- **fix:** Various shell quoting, lint, and argument-order fixes across adapters and skills

_Generated by oflow e2e verification._
