# Release Notes

## Recent Changes

- **GitLab board adapter**: Added `GitLabBoardAdapter` supporting `OFLOW_BOARD=gitlab` with label-based workflow management, task listing/claiming/updating, and mocked unit tests
- **Opencode agent adapter**: Upgraded `OpencodeAdapter` stub to a fully working implementation with accurate exit code tracking and unit tests; documented `OFLOW_AGENT=opencode` single-context limitation
- **Run reporting**: Daemon now shows `[active]`/`[done]` status with token counts; `oflow report estimate` persists complexity/timing; artifacts auto-emitted from `StateManager.writeArtifact`; skill steps emit step events
- **Multiagent code review skill**: Added parallel specialist agents (logical, smells, tests, assembly) extracted into `.claude/agents/` files; skill acts as lean orchestrator dispatching agents by name
- **Dynamic version**: CLI version now read from `package.json` via `createRequire` instead of being hardcoded
- **Schema consolidation**: Duplicate schema files merged into a single `schemas.ts`
- **Bug fixes**: Removed unused imports, fixed session loop when `oflow-in-progress` label already removed, fixed `--fork-point` argument order in code-review step
- **README**: Added installation and usage documentation

_Generated by oflow e2e verification._
