# Release Notes

## Recent Changes

- **fix:** Status command now works correctly — `writeSessionsJson` is wired into the daemon loop so active sessions are persisted and visible via `oflow status`
- **fix:** Task failure now restores `oflow-ready` label instead of incorrectly applying `oflow-done`
- **feat:** Dev workflow git setup moved to Step 0, ensuring a clean branch start before any exploration or code changes
- **feat:** GitLab board adapter implemented with full `BoardAdapter` interface, label-based workflow management, and unit tests
- **feat:** OpencodeAdapter upgraded from stub to full implementation with exit code tracking and tests
- **feat:** Run reporting added — daemon shows `[active]`/`[done]` status with token counts, `oflow report estimate` persists complexity/timing, artifacts auto-emit events, and skill steps emit progress events
- **refactor:** Multiagent code review skill refactored to use standalone `.claude/agents/` files (`review-logical`, `review-smells`, `review-tests`, `review-assembly`) instead of inline prompts
- **feat:** Multiagent code review skill added with parallel specialist agents fanning out then assembling a single report
- **chore:** CLI version now read dynamically from `package.json` to avoid version drift
- **docs:** README added with installation and usage instructions

_Generated by oflow e2e verification._
