# Release Notes

## Recent Changes

- **fix**: Restore `oflow-ready` label on task failure instead of `oflow-done`, ensuring failed tasks are correctly re-queued
- **feat**: Move git setup to Step 0 in `dev-workflow` to guarantee a clean branch start before any implementation work
- **feat**: Implement `GitLabBoardAdapter` with full `BoardAdapter` interface, adding GitLab issues board support
- **feat**: Implement `OpencodeAdapter` with exit code tracking and tests, enabling the Opencode agent adapter
- **feat**: Add run reporting with heartbeat, estimates, artifacts, and step events for richer task observability
- **feat**: Add multiagent code review skill with parallel specialist agents for higher-quality reviews
- **refactor**: Extract inline agent prompts into standalone agent files for better maintainability
- **refactor**: Consolidate schema files into a single `schemas.ts` to reduce duplication
- **fix**: Various shell safety fixes (quote vars, use `wc -c` for empty checks) and code-review step improvements
- **chore**: Read version from `package.json` instead of hardcoding in CLI; merge split `fs/promises` imports

_Generated by oflow e2e verification._
