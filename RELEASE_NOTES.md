# Release Notes

## Recent Changes

- **fix: session stuck in loop** — swallow 404s from `removeLabel` and wrap `board.updateTask` in try/finally so sessions are always cleaned up even when label removal fails
- **refactor: shared test fixtures** — extract `makeTask()`, `makeMockAdapter()`, and `makeMockStateManager()` into `src/test-utils.ts` to eliminate copy-paste across test files
- **fix: Zod validation + agent log reliability** — validate `TaskContext` with `TaskContextSchema.parse()`, fix agent log writing via direct fd stdio, and correct all dev-workflow step skill paths
- **feat: e2e verification** — add `scripts/e2e.sh` that builds oflow, creates a real GitHub issue, spawns the daemon, and polls for a PR to validate the full pipeline end-to-end
- **fix: daemon session logging** — only log newly started sessions per poll cycle, not all active sessions on every iteration
- **fix: claude spawn + log streaming** — spawn claude via stdin to avoid YAML frontmatter parsing issues; pipe agent stdout/stderr to log file and terminal in real-time with `[task-X]` prefix
- **feat: label-based task filtering** — add `--label` option to `board list`, `board pick`, and `run` commands for filtering tasks by custom labels
- **fix: skills restructured as folders** — each skill is now a folder with `SKILL.md` matching the agentskills.io open format; daemon poller updated accordingly
- **fix: skills discovery + dotenv isolation** — move skills to `.claude/skills/` for native Claude Code discovery; isolate `dotenv.config()` to CLI entry point only
- **feat(M0–M4): full MVP scaffold** — TypeScript project, GitHub board adapter, state manager, workflow skills, agent adapters, daemon scheduler/poller, and full CLI wiring

_Generated by oflow e2e verification._
