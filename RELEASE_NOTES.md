# Release Notes

## Recent Changes

- **OpencodeAdapter**: Fully implemented with accurate exit code tracking, unit tests, and README documentation for `OFLOW_AGENT=opencode`
- **Run reporting**: Daemon now shows `[active]`/`[done]` status with token counts; `oflow report estimate` persists complexity/timing; artifacts auto-emitted from `StateManager.writeArtifact`; skill steps emit progress events
- **Multiagent code review**: New skill with three parallel specialist agents (logical, smells, tests) assembled into a single deduplicated report; agent prompts extracted to standalone `.claude/agents/` files
- **Dynamic version**: CLI version now read from `package.json` instead of hardcoded to prevent drift
- **Schema consolidation**: Duplicate schema files merged into a single `schemas.ts`
- **Bug fixes**: Fixed session loop on already-removed label, Zod validation for `readTaskContext`, shell quoting in skill scripts, and `--fork-point` argument order in code-review step
- **Code quality**: Removed unused imports, extracted duplicate test fixtures to shared file, cleaned up stale worktree

_Generated by oflow e2e verification._
