# Release Notes

## Recent Changes

- **Zod validation & reliability fixes**: Added strict schema validation for `readTaskContext`, fixed agent log writing to use direct fd stdio for reliable disk output, and corrected all dev-workflow step skill paths.
- **E2E verification pipeline**: Added `scripts/e2e.sh` and a `release-notes` workflow skill as the e2e test target — validates the full oflow pipeline (build → GitHub issue → daemon → PR) before any dev-workflow PR is opened.
- **Daemon spawn & logging improvements**: Fixed Claude Code adapter to spawn via stdin, stream logs to both file and terminal in real-time with `[task-X]` prefix, and track exit codes accurately.
- **Label-based task filtering**: Added `--label` option to `board list`, `board pick`, and `run` commands, threading the filter through to the GitHub API.
- **Skills restructured to folder format**: Each skill is now a `<name>/SKILL.md` folder per the agentskills.io open format; moved to `.claude/skills/` for native Claude Code discovery.
- **Config & test isolation**: Removed `dotenv.config()` from the config loader so tests are not polluted by local `.env`; moved skills to `.claude/skills/`.
- **CI fixes**: Added `package-lock.json` for `npm ci`, resolved ESLint errors blocking CI, and excluded `.worktrees` from Vitest discovery.
- **Core milestones (M0–M4)**: Scaffolded TypeScript project, GitHub board adapter, state manager with artifact schemas, workflow skills for dev-workflow, and agent adapters with daemon scheduler/poller and full CLI wiring.

_Generated by oflow e2e verification._
