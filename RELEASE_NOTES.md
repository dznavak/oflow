# Release Notes

## Recent Changes

- **Jira board adapter**: Added full `BoardAdapter` integration for Jira with macOS keychain token support, factory pattern for board selection, and 23 new tests
- **GitLab board adapter**: Implemented `GitLabBoardAdapter` with the full `BoardAdapter` interface
- **Per-step timeout & hang detection**: Daemon loop now kills steps exceeding the configurable timeout (default 900s), marks runs as failed, and releases session slots
- **Event-driven completion**: Replaced `setInterval` polling in `ClaudeCodeAdapter` and `OpencodeAdapter` with event-driven resolution on process close, eliminating hang risk
- **Run reporting**: Added heartbeat, estimates, artifacts, and step events to run reports for better observability
- **Config cleanup**: Removed dead `agentModel` config field and `OFLOW_AGENT_MODEL` env var; documented how model selection actually works
- **Dead code removal**: Deleted unused `spawner.ts` (replaced by `poller.ts`) and removed dead `agentModel` references
- **Refactors**: Extracted duplicate `getCurrentTaskId` to a shared utility; removed `process.exit` from `validateArtifactCmd` to improve testability
- **Agent prompts**: Moved inline agent prompts into standalone agent files for better maintainability
- **Lint fixes**: Prefixed unused variables and removed unused imports throughout

_Generated by oflow e2e verification._
