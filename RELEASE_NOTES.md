# Release Notes

## Recent Changes

- **docs**: Added README with installation and usage instructions
- **feat**: Added e2e verification step to dev-workflow — runs `scripts/e2e.sh` to validate the full oflow pipeline before opening a PR
- **feat**: Added label-based task filtering for `board list`, `board pick`, and `run` commands via `--label` option
- **fix**: Resolved session stuck-in-loop bug when `oflow-in-progress` label was already removed (404 on label removal now treated as success)
- **fix**: Fixed Claude agent spawn via stdin, real-time log streaming with `[task-X]` prefix, and improved daemon startup logs
- **fix**: Zod validation for `readTaskContext`, more reliable agent log writing via fd stdio, and corrected dev-workflow skill paths
- **refactor**: Consolidated 6 schema files into a single `src/state/schemas.ts`
- **refactor**: Extracted duplicate test fixtures to a shared `src/test-utils.ts`
- **chore**: Removed stale `.worktrees/mvp` worktree after branch was merged

_Generated by oflow e2e verification._
