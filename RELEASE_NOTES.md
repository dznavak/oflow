# Release Notes

## Recent Changes

- **Run reporting**: Added heartbeat, estimates, artifacts, and step events to run reporting for better observability
- **Multiagent code review**: New skill with parallel specialist agents (logical, smells, tests) and an assembly step
- **Agent prompts**: Extracted inline agent prompts into standalone files for maintainability
- **Dev workflow**: Added e2e verification step to the dev-workflow skill
- **OpenCode adapter**: Fixed child process lifecycle alignment with ClaudeCodeAdapter; merged split fs/promises imports
- **Schema consolidation**: Consolidated schema files into a single `schemas.ts`
- **Bug fixes**: Fixed session loop when `oflow-in-progress` label is already removed; fixed Zod validation for `readTaskContext`; fixed agent log reliability and skill path resolution
- **Tooling**: Version now read from `package.json` instead of being hardcoded; added README with installation and usage instructions

_Generated by oflow e2e verification._
