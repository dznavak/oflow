# Release Notes

## Recent Changes

- **Multi-agent code review skill**: Added parallel specialist agents (logical, smells, tests, assembly) that fan out independently and produce a single deduplicated report
- **Agent prompt extraction**: Moved inline agent prompts from the multiagent-code-review skill into standalone `.claude/agents/` files for cleaner orchestration
- **Code-review step improvements**: Updated to use merge-base diff and added review principles; fixed `--fork-point` argument order with fallback
- **E2E verification step**: Added e2e verification step to the dev-workflow skill for automated workflow testing
- **Dynamic version reading**: CLI now reads version from `package.json` at runtime instead of a hardcoded string, preventing version drift
- **Schema consolidation**: Merged duplicate schema files into a single `schemas.ts` for cleaner module boundaries
- **Reliability fixes**: Fixed Zod validation for `readTaskContext`, agent log reliability, skill path resolution, and daemon spawn-log loop
- **Session loop fix**: Resolved issue where sessions got stuck in a loop when the `oflow-in-progress` label was already removed
- **Opencode adapter fix**: Removed `child.unref()` from `OpencodeAdapter` to align behavior with `ClaudeCodeAdapter`
- **README**: Added installation and usage documentation at the repo root

_Generated by oflow e2e verification._
