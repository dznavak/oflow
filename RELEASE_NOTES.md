# Release Notes

## Recent Changes

- **Event-driven polling**: Replaced `setInterval` with event-driven resolution in `waitForCompletion` to eliminate busy-waiting
- **GitLab support**: Implemented `GitLabBoardAdapter` with full `BoardAdapter` interface for GitLab project boards
- **Opencode adapter**: Added `OpencodeAdapter` with exit code tracking and tests alongside the existing Claude Code adapter
- **Run reporting**: Added heartbeat, estimates, artifacts, and step events to run reporting for better observability
- **Git setup in dev-workflow**: Moved git setup to Step 0 to ensure a clean branch before any work begins
- **Multiagent code review**: Added parallel specialist code review skill that fans out to three agents and assembles one final report
- **Refactors and cleanups**: Extracted duplicate `getCurrentTaskId` utility, removed unused config params, extracted inline agent prompts into standalone files, and removed `process.exit` from validation commands
- **Bug fixes**: Restored `oflow-ready` label on task failure, wired `writeSessionsJson` into daemon loop so the `status` command works correctly

_Generated by oflow e2e verification._
